[tool.poetry]
name = "lambda"
version = "0.1.0"
description = ""
authors = ["brian amaral"]

[tool.poetry.dependencies]
python = "^3.9"
boto3 = "^1.18.11"
pandas = "^1.3.1"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
